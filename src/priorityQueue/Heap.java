package priorityQueue;

// an implementation of a min Heap using an array

public class Heap {

	private int size;
	private int[] heapArray;

	public Heap(int capacity) throws Exception {
		if (capacity <= 0)
			throw new Exception("Heap size must be > 0.");

		size = 0;
		heapArray = new int[capacity];
	}
	
	
	// -- public methods --
	
	// get
	
	// insert
	
	// remove

	// -- private ones --
	
	
	
	
	
}
